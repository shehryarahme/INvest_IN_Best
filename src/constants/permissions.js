// import { Platform } from 'react-native';
// import { request, PERMISSIONS } from 'react-native-permissions';
// import messaging from '@react-native-firebase/messaging';

// export const requestPNPermission = async () => {
//     const pm_object = {
//         alert: true,
//         announcement: true,
//         badge: true,
//         carPlay: true,
//         provisional: true,
//         sound: true,
//       }
//     const isAllowed = await messaging().requestPermission(pm_object);
//     return isAllowed ? await messaging().getToken().then(token => { return token }) : null;
// }

